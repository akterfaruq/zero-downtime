apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-green
  labels:
    app: zero-demo
    track: green
spec:
  replicas: 3
  selector:
    matchLabels:
      app: zero-demo
      track: green
  template:
    metadata:
      labels:
        app: zero-demo
        track: green
    spec:
      containers:
      - name: web
        image: akterfaruq/zero-downtime-demo:v2
        env:
        - name: APP_VERSION
          value: "v2"
        readinessProbe:
          httpGet:
            path: /ready
            port: 3000
          initialDelaySeconds: 2
          periodSeconds: 2
        livenessProbe:
          httpGet:
            path: /healthz
            port: 3000
          initialDelaySeconds: 10
          periodSeconds: 10
        ports:
        - containerPort: 3000
